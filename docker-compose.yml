services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - UPLOAD_DIR=/app/uploads
    volumes:
      - uploads_data:/app/uploads

volumes:
  uploads_data:
